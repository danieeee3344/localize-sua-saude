# ============================================
# Localize Sua Saúde - Frontend (EXEMPLO)
# ============================================
# Por segurança (A5), NENHUMA chave é embutida no bundle do navegador.
# O POST /api/leads é liberado pela origem (ORIGENS_CONFIAVEIS no .env da api).
# A API_KEY_ADMIN nunca deve aparecer em código frontend!

VITE_API_BASE_URL=/api
